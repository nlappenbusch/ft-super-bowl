apiVersion: v1
kind: Service
metadata:
  name: qr-billing-service-svc
  namespace: qr-billing-service
spec:
  selector:
    app: qr-billing-service
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30503